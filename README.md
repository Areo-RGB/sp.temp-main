# Trapwire Racing

A realtime controller and client camera setup for a virtual finish line.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Configure Firebase in `src/firebase.ts` for your project.
3. Run the app:
   `npm run dev`
